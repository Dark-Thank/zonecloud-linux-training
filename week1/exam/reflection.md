Trong tuần 1 em đã học được các lệnh Linux cơ bản để thao tác với file và thư mục như ls, cd, cp, mv, rm và mkdir. 
Em hiểu thêm về cấu trúc filesystem của Linux như /etc, /var, /home và /tmp. 
Ngoài ra em đã thực hành quản lý user, group và permission bằng chmod, chown, adduser và usermod. 
Em cũng học cách quản lý process và service bằng ps, top, systemctl và nginx. 
em đã làm quen với việc đọc log hệ thống trong /var/log và xử lý text bằng grep, awk, sort, wc và tail. 
Hiểu được cách hoạt động của SSH key, network cơ bản và các port dịch vụ như SSH và nginx. 
Học được cách troubleshoot lỗi permission và service. 
Sau tuần đầu tiên em cảm thấy quen hơn khi dùng Linux và các câu lệnh được học 
