Sau khi làm bài tập này em học được những thứ như sau 

NGINX là gì — web server, application gateway, reverse proxy
Cài đặt — Mainline vs Stable, Ubuntu 
Lệnh và file quan trọng — nginx -t, reload, cấu trúc thư mục
Web server cơ bản — server block, listen, root, index
SSL/HTTPS — redirect HTTP -> HTTPS, certificate, Let's Encrypt
PHP-FPM — reverse proxy xử lý PHP
Load Balancing — Round Robin, Least Connections, IP Hash
Caching — proxy_cache, cache_lock, cache_revalidate
Tối ưu hiệu suất — Keepalive, HTTP/2, SSL session cache, worker processes
Health Checks & Monitoring — stub_status, Prometheus, Grafana
